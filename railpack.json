{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "playwright": {
      "inputs": [{ "step": "install" }],
      "commands": ["/app/.venv/bin/playwright install --with-deps chromium"]
    }
  },
  "deploy": {
    "inputs": [{ "step": "playwright" }],
    "startCommand": "/app/.venv/bin/python3 boxboxai_bot.py"
  }
}
